"""Figure

To be implemented for benchmarking:
  - plot_r2_curves(histories): R^2 vs epoch, one curve per version, over diffrent datasets.
  - summary_table(aggregates): recap table (R^2, time, epochs) per version/dataset.
  - create a plot representation to check if b is going toward {0, 1}.
"""
